// execute coe in vscode for a javascript file
console.log("hello world");

//how do we use the debugger to check out our code

for (let i = 0; i < 5; i++) {
    console.log(`i is now: ${i}`)
}

function add(x,y) {
    return x + y;
}

add(1,2);


//debugger
    // break points, click on left hand side beyond the number (red dot), pause code at this point
    // congrols - stepping through execution
    // watch you variables